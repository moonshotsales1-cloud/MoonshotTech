export const metadata = {
  title: "Branding Services - Moonshot Animations",
  description: "Get in touch with Moonshot Animations for your branding needs.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
