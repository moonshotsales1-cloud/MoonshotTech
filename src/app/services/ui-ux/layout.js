export const metadata = {
  title: "UI UX Services - Moonshot Animations",
  description:
    "Get in touch with Moonshot Animations for your UI UX design needs.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
