export const metadata = {
  title: "Email Marketing Services - Moonshot Animations",
  description:
    "Get in touch with Moonshot Animations for your email marketing needs.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
